Use the Linux tree command to generate an index of all the files in a directory and, optionally, subdirectories. Use this command for building an HTML index of the HTML files in the current directory:

 tree -H '.' -L 1 --noreport --charset utf-8 -P "*.html" -o index.html